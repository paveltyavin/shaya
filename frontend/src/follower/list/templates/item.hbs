<div class="text-center">
    <a class="username">
        {{username}}
    </a>
    <div class="img-thumbnail">
        <img src="{{profile_picture}}" alt="">
    </div>
    {{#if inactive_date}}
        {{inactive_date}}
    {{/if}}
</div>